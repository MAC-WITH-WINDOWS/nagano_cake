<%= stylesheet_link_tag "application", :media => "all" %>
<div class="container">
  <div class="row">
    <h3 class="bg-secondary bg-opacity-50 m-5">ショッピングカート</h3>
  </div>
  <div class="row">
    <table align="center">
      <tr>
        <th colspan="3">商品名</th>
        <th>単価(税込)</th>
        <th>数量</th>
        <th>小計</th>
        <th></th>
      </tr>
      <% @cart_items.each do |item| %>
      <td><%= item.name %></td>
    </table>
  </div>

</div>